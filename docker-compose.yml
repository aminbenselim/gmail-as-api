services:
  gmail-as-api:
    image: ghcr.io/aminbenselim/gmail-as-api:latest
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
